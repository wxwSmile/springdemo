package com.gupao.servlet.framework.annotation;

public enum GpRequestMethod {
    GET,
    HEAD,
    POST,
    PUT,
    PATCH,
    DELETE,
    OPTIONS,
    TRACE;

    private GpRequestMethod() {
    }
}
